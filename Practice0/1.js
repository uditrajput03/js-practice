let distanceKM = 5;
let distanceM = distanceKM * 1000;
let distanceCM = distanceM * 100;

// console.table([distanceKM,distanceM,distanceCM]);
console.log(`Distance in meter is ${distanceM}m`);
console.log(`Distance in centi-meter is ${distanceCM}cm`);